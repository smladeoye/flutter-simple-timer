name: simple_timer
description: A Simple Timer Widget that display the timer progress with various customizable options.
version: 1.0.0
homepage: https://github.com/smladeoye/flutter-simple-timer

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
